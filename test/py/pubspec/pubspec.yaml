name: sample
description: "버전업테스트"
publish_to: 'none'
version: 1.2.4+6

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  # Android 플랫폼 채널과 실제 로컬 저장소를 검증한다.
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
